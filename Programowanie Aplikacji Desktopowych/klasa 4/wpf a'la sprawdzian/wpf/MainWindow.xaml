<Window x:Class="wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:BookEntryViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBox Text="{Binding Author}"/>
            <TextBox Text="{Binding Title}"/>
            <TextBox Text="{Binding Year}"/>
            <TextBox Text="{Binding Pages}"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <Button Command="{Binding ZapiszCommand}" Content="zapisz"/>
            <Button Command="{Binding WyczyscCommand}" Content="wyczysc"/>
            <TextBlock Text="{Binding Komunikat}"/>
        </StackPanel>
        
    </Grid>
</Window>
