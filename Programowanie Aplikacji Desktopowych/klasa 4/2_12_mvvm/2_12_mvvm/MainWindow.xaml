<Window x:Class="_2_12_mvvm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_2_12_mvvm"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:TodoListViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox Text="{Binding InputText}" 
                Width="200" Height="30" Margin="10"/>

            <Button Content="Dodaj" 
                Command="{Binding AddTodoCommand}" 
                Width="100" Height="30" Margin="10"/>

            <Button Content="Usuń" 
                Command="{Binding RemoveCheckedTodosCommand}"
                Width="100" Height="30" Margin="10"/>
            
        </StackPanel>

        <ListBox x:Name="lista" ItemsSource="{Binding Items}" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" >
                        <CheckBox Content="{Binding Text}" IsChecked="{Binding IsDone}" VerticalAlignment="Center" FontSize="18"/>
                        
                        <Button Content="Usuń" 
                            Command="{Binding DataContext.RemoveTodoCommand, ElementName=lista}"
                                CommandParameter="{Binding}"
                            Margin="10 0 0 0" FontSize="18"/>
                        
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
